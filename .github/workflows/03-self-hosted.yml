name: Self Hosted Test

on:
  workflow_dispatch:

jobs:
  test-runner:
    runs-on: self-hosted

    steps:
      - name: Checkout
        uses: actions/checkout@v4 # Atualizado para v4 (estável) se a v5 não for estritamente necessária

      - name: Test Runner
        run: |
          echo "Executando no self-hosted runner" 
          hostname 
          pwd
